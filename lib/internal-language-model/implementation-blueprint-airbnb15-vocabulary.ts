/** Phase Airbnb 15 — Hosts Guest Center vocabulary. */
export const AIPIFY_HOSTS_GUEST_CENTER_POSITIONING =
  "Complete overview of every guest and guest interaction — understand active guest situations from one workspace.";

export const AIPIFY_HOSTS_GUEST_CENTER_TERMINOLOGY = {
  guestCenter: "Hosts Guest Center",
  guestCenterFoundation: "Guest Center Foundation",
} as const;

export const AIPIFY_HOSTS_GUEST_CENTER_FORBIDDEN = [
  "AI assistant",
  "AI chatbot",
  "AI guest",
  "autonomous agent",
] as const;

export const AIPIFY_HOSTS_GUEST_CENTER_ROUTE = "/app/aipify-hosts/guests";

export const AIPIFY_HOSTS_GUEST_CENTER_SECTIONS = [
  "active_guests",
  "upcoming_guests",
  "guest_history",
  "guest_requests",
  "guest_notes",
  "guest_timeline",
] as const;
