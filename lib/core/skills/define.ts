import type { SkillDefinition } from "./types";

/** Type-safe skill definition helper for registry entries. */
export function defineSkill(definition: SkillDefinition): SkillDefinition {
  return definition;
}
